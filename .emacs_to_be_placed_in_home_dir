;; rename this file to .emacs and place it in your home folder.
;; below make sure that the init.el link points to the init.el from inside this .emacs.d (dotfiles) repo.

(setq user-emacs-directory "~/")
(load (concat user-emacs-directory ".emacs.d/init.el"))

